# Using the MCP Gateway with Docker in Docker

This example shows how to run the MCP Gateway and the MCP Servers in a single Docker in Docker container.

## How to run

```console
docker compose up
```
