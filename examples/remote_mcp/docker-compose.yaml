services:
  gateway:
    image: docker/agents_gateway:v2
    volumes:
      - ./catalog.yaml:/mcp/catalog.yaml
    command:
      - --catalog=/mcp/catalog.yaml
      - --servers=gitmcpmoby
      - --transport=sse