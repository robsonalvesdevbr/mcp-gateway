services:
  gateway:
    image: docker/agents_gateway:v2
    ports:
      - "8811:8811"
    volumes:
      - ./catalog.yaml:/app/catalog.yaml
    command:
      - --catalog
      - /app/catalog.yaml
      - --servers=gitmcpmoby
      - --transport=sse